{
  "name": "irm-extra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "pages:build": "vercel build",
    "preview": "npx wrangler pages dev .vercel/output/static --d1=DB --r2=BUCKET",
    "deploy": "npm run pages:build && npx wrangler pages deploy .vercel/output/static"
  },
  "dependencies": {
    "next": "^15.3.0",
    "react": "^19",
    "react-dom": "^19",
    "@cloudflare/next-on-pages": "^1.13.16",
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5",
    "eslint": "^9",
    "eslint-config-next": "^15.3.0",
    "wrangler": "^4.0.0"
  }
}
